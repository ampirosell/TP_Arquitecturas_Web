version: "3.8"

services:
  viaje-service:
    build: .
    container_name: viaje_service
    environment:
      DB_HOST: inte4_mysql
      DB_PORT: 3306
      DB_NAME: viaje
      DB_USER: integrador
      DB_PASSWORD: integrador123
    depends_on:
      - mysql
    networks:
      - inte4_net
    ports:
      - "8081:8080"
    restart: unless-stopped

networks:
  inte4_net:
    external: true
